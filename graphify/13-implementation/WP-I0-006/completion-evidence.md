# WP-I0-006 completion evidence

- Package: WP-I0-006 — Build-output isolation decision
- Requirement: `CAN-MISSION-I0-005`
- Starting SHA: `82dfd9e8e2b3a826d5728cc3b2290b3aedb3e50c`
- Selection: the sole explicit authorization, independently confirmed READY from the canonical DAG.
- Files changed by the package: the decision, validator/collector, and evidence in this directory; existing Graphify certification outputs may be regenerated by the repository's certification tool.

## Requirement trace

`CAN-MISSION-I0-005` → `build-output-isolation-decision.json` → decision validation plus 31 typed negative probes → `verification-report.json` → four exit-gate clauses.

The decision authorizes no output-producing action now. A later package must separately authorize one run-scoped absolute root outside the repository, map build/test/cache/generated/package-manager/temporary outputs below it, redirect every tool write, and reject the command if complete redirection cannot be proven. The existing checkout is the source workspace; cloning, worktrees, backups, archives, and duplicate trees are forbidden.

## Validation

- Focused: **PASS** — decision structure and all boundary invariants validated.
- Negative: **PASS** — 31/31 invalid decisions produced the expected typed error; state fingerprint preserved.
- Regression: **PASS** — WP-I0-001 remains COMPLETE/PASS and 3697 Codebase files match its SHA-256 baseline.
- Artifact scan: **PASS** — zero current workspace/copy/build/test/cache/generated/package-manager artifacts and zero outside-Graphify changes.


## Prior failed invocation

`prior-failed-run-incident.md` preserves the previous invocation's disclosed external stdout-capture failure. That run was not committed or certified. This invocation streams validation output and is evaluated independently.


## Exit gate

- **PASS** — The later writable boundary is explicit: The decision requires a separately authorized, run-scoped absolute output root outside the repository and maps all six output classes to unique children.
- **PASS** — No workspace, repository copy, build, test, cache, generated-code, package-manager, or temporary output was created: The isolated output environment is unbound; output execution and workspace creation are false; the recursive artifact scan reports zero forbidden classes and zero outside-Graphify changes for this invocation.
- **PASS** — Invalid input returns typed errors with no partial authoritative state: All 31 invalid decisions returned their expected typed errors and the before/after repository fingerprints match.
- **PASS** — The verified read-only provenance baseline remains intact: All 3697 Codebase files match the WP-I0-001 path, size, and SHA-256 manifest; WP-I0-001 status and exit gate remain PASS.
