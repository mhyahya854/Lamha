# WP-I0-007 authoring incident

- Before the authorized baseline run began, `python -m py_compile graphify/13-implementation/WP-I0-007/collect_evidence.py` created `graphify/13-implementation/WP-I0-007/__pycache__/collect_evidence.cpython-311.pyc` despite `PYTHONDONTWRITEBYTECODE=1`; `py_compile` writes explicitly.
- The exact generated file and now-empty directory were removed immediately with path-resolved, non-recursive filesystem calls.
- The command is classified as an unauthorized authoring-time cache write, is not package validation or exit-gate evidence, and will not be repeated.
- The separately authorized baseline run had not started at that point and its external root did not exist. All subsequent syntax checks use non-writing AST parsing. The later run-002 isolation violation and recovery are disclosed below.

## Superseded isolated run

- `run-20260809-001` completed preservation and cleanup, but its runner tried to start the resolved Windows `pnpm.cmd` shim as a native executable. Sixteen Node attempts were therefore recorded as unavailable instead of executing the package-manager shim.
- That result was rejected as insufficient coverage before review. Its external root was removed, no Codebase path changed, and its generated evidence was superseded by the later corrected run.

- `run-20260809-002` is rejected as an **ISOLATION_VIOLATION**. Invoking the bundled Windows pnpm shim without an installed project dependency tree populated the authorized external cache, created the start-absent ignored `Codebase/node_modules` tree containing 5,597 reparse entries, and reached the first command timeout.
- The run was stopped. The exact external root was removed; the exact start-absent generated `Codebase/node_modules` tree was removed without changing application source; all 3,697 Codebase paths, sizes, and SHA-256 values then matched the WP-I0-001 baseline; all run-001/run-002/run-003 external roots were confirmed absent before retry.
- Run-002 is failure/recovery evidence only and must never be cited as an acceptable isolated baseline attempt.
- `run-20260809-003` cleanly treated every Node surface as a typed `PROJECT_DEPENDENCIES_UNAVAILABLE` pre-execution blocker after proving `Codebase/node_modules` and package-local tool binaries were absent. It did not claim those commands executed. Before review, its preservation scanner was hardened to reject ignored generated directories and reparse trees directly rather than traversing them.
- `run-20260809-004` used that hardened scan, then failed independent review because its surface list substituted commands, omitted manifest-declared suites, left latent Flutter writes unsafe, missed protected Graphify paths, leaked an untyped file-root error, and emitted stale run-003 prose.
- `run-20260809-005` is the repaired candidate. It derives every non-watch `build*`/`test*` Node script from source manifests, adds the mobile UI and platform surfaces, blocks unsafe commands before execution, validates WP-I0-006, protects the repository outside exact write allowlists, and derives all current-run evidence from authorization.
- Run-005 then failed its real reparse scanner fixture because Windows denied unprivileged symbolic-link creation (`WinError 1314`). Its source preservation and cleanup passed, but the evidence remained FAIL.
- `run-20260809-006` explicitly authorizes a directory-junction fixture through `cmd.exe /d /c mklink /J`, allowing the real reparse detector to be exercised without elevated symlink privilege.
- Run-006 passed its generated checks but failed independent review: path type errors escaped the typed boundary, the source oracle reused build-plan discovery and missed the E2E mise container build, mandatory command semantics were not compared, the ML test lost coverage/cwd fidelity, and the package allowlist accepted a sibling prefix. Its evidence is rejected and superseded.
- `run-20260809-007` started from the unchanged 3,697-file Codebase baseline. It added typed path fixtures, an independently parsed package/mise surface oracle with full semantic tuples for exact-name mise tasks including `e2e/mise.toml`'s container build, faithful ML coverage execution from the ML working directory, and an exact slash-bounded package allowlist fixture.
- Run-007 then failed independent review because JSON-valid NUL path strings could still escape as raw filesystem exceptions and the mise inventory recognized only exact `build`/`test` task names, omitting prefixed variants. Its evidence is rejected and superseded.
- `run-20260809-008` rejects control-bearing paths before filesystem access, translates remaining path-resolution failures to typed errors, and independently inventories every mise task matching `^(build|test)([-:]|$)`, including E2E `test-web`, server `test-medium`, web `build-stats`, and mobile `build:android`.
- Run-008 then failed independent review because the output-root validator still accepted Windows reserved names, alternate-data-stream syntax, trailing-dot aliases, and other noncanonical spellings, while task-name-only discovery missed root mise `prod` even though its command builds the production server and ML images. Its evidence is rejected and superseded.
- `run-20260809-009` validates every Windows output component and canonical spelling before filesystem access, with exact reserved/ADS/trailing-dot/parent-alias fixtures, and content-qualifies Docker Compose build-producing mise commands regardless of task label, including `prod` with an explicit runtime/volume/network blocker.
- Run-009 then failed independent review because both mise parsers ignored table-form task references whose `args` inject `--build`, omitting four real container-build wrappers. Its evidence is rejected and superseded.
- `run-20260809-010` independently parses string, list, and task-reference-table mise run forms, resolves `--build` wrapper semantics, and records exact blocked attempts for `dev-update`, `dev-scale`, `prod-scale`, and `e2e-update`.
- Run-010 then failed independent review because output-root validation resolved aliases before inspecting the lexical component chain, allowing existing Windows junction paths to hide their reparse point. Its evidence is rejected and superseded.
- `run-20260809-011` checks every existing lexical component with `lstat` before resolution and extends the authorized in-root real junction fixture to require the typed `OUTPUT_ROOT_REPARSE` result.
- Run-011 then failed independent review because the reserved-device set omitted Windows superscript COM/LPT aliases and console device names. Its evidence is rejected and superseded.
- `run-20260809-012` validates the full DOS device family (`CLOCK$`, `CONIN$`, `CONOUT$`, COM/LPT digits and superscript ¹/²/³ aliases), enforces portable component length, and includes exact negative fixtures for each newly attacked class.
- Run-012 then failed independent review because the Win32 `\\?\` extended namespace preserved an alternate spelling that bypassed ordinary-path repository overlap comparison. Its evidence is rejected and superseded.
- `run-20260809-013` rejects Win32 device and extended namespace anchors (`\\?\`, `\\.\`, and `\??\`) before normalization, with the exact nonexistent in-repository alias fixture.
- Run-013 then failed independent review because forward-slash `//?/` normalized into the same extended namespace only after the literal-prefix check. Its evidence is rejected and superseded.
- `run-20260809-014` normalizes slash direction for namespace detection before any `Path` construction and includes the exact `//?/C:/...` in-repository alias fixture.
- Run-014 then failed independent review because loopback UNC administrative-share spellings could address the repository while comparing unequal to its drive path. Its evidence is rejected and superseded.
- `run-20260809-015` rejects all UNC roots before filesystem access because network authorization is false, with exact `localhost` and `127.0.0.1` repository-alias fixtures.
- Run-015 then failed independent review because Python equality allowed floating JSON `schemaVersion: 1.0` to satisfy the integer-one contract. Its evidence is rejected and superseded.
- `run-20260809-016` requires the exact integer type for `schemaVersion` and includes a raw JSON floating-version fixture returning `AUTHORIZATION_VERSION_INVALID`.
- Run-016 then failed independent review because the safe root was not cross-bound to the authorization's package and run identities. Its evidence is rejected and superseded.
- `run-20260809-017` derives the reviewed isolated-output base and requires the exact `${BASE}/WP-I0-007/${runId}` root, with mismatched-run, mismatched-package-root, and arbitrary-root fixtures; it also removes only empty template parents created by the run and gates their cleanup.
- Run-017 was rejected by the local post-run audit before independent certification: template-parent creation flags were captured after root creation, so two empty authorized parents remained and were incorrectly labeled preexisting. The exact empty package parent and isolated base were verified, removed non-recursively, and confirmed absent; no repository path was involved.
- `run-20260809-018` captures template-parent existence before root creation and gates both exact parent removal and absence after cleanup.
- Run-018 then failed independent review because output-root and child creation still occurred before the cleanup-protected region, leaving a partial-setup failure path untyped and unrecovered. Its evidence is rejected and superseded.
- `run-20260809-019` protects the first filesystem mutation, translates setup/execution/cleanup failures, and performs an injected failure after the first output child is created; execution proceeds only after the exact run root and newly created template parents are proven absent.
