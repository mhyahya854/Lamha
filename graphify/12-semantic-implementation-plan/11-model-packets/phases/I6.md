# I6 — Events and Manage Later

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Implement review-first event records, proposals, organization, and reversible filesystem materialization plans.
- Entry gate: Asset identity, review, and transaction-plan contracts exist.
- Exit gate: Event workflows preserve linked folders and never mutate without confirmed current plans.

## Reviewed work packages

- `WP-I6-001` — Manage Later non-mutating scan: Implement and verify the bounded manage later non-mutating scan surface.
- `WP-I6-002` — Event authoritative records: Implement and verify the bounded event authoritative records surface.
- `WP-I6-003` — Manual event creation: Implement and verify the bounded manual event creation surface.
- `WP-I6-004` — Event inference proposals: Implement and verify the bounded event inference proposals surface.
- `WP-I6-005` — Event merge plan: Implement and verify the bounded event merge plan surface.
- `WP-I6-006` — Event split and link plans: Implement and verify the bounded event split and link plans surface.
- `WP-I6-007` — Event naming and normalization: Implement and verify the bounded event naming and normalization surface.
- `WP-I6-008` — Date and location boundaries: Implement and verify the bounded date and location boundaries surface.
- `WP-I6-009` — Event filesystem materialization plan: Implement and verify the bounded event filesystem materialization plan surface.
- `WP-I6-010` — Event and Manage Later UI: Implement and verify the bounded event and manage later ui surface.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
