# I0 — Repository Proof and Feasibility

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Preserve provenance and establish a reproducible non-destructive baseline before application changes.
- Entry gate: Active plan, source hashes, and write boundaries are verified.
- Exit gate: Repository/toolchain/component facts and baseline attempts are recorded without changing application source.

## Reviewed work packages

- `WP-I0-001` — Read-only repository provenance and integrity baseline: Inspect the existing repository without mutation and record provenance, file inventory, SHA-256 integrity, available Git state, and toolchain/manifest evidence only inside Graphify.
- `WP-I0-002` — Read-only Git-state inspection: Inspect existing Git state without creating or modifying commits, branches, tags, stashes, worktrees, the index, or configuration.
- `WP-I0-003` — Existing repository SHA-256 manifest: Calculate and verify path, size, and SHA-256 evidence for existing repository files without creating an archive, backup, or duplicate tree.
- `WP-I0-004` — Mixed-version manifest investigation: Implement the reviewed Mixed-version manifest investigation for requirements CAN-MISSION-I0-004 covering 1 requirements: Investigate and record mixed-version package, lockfile, gene.
- `WP-I0-005` — Toolchain inventory: Implement the reviewed Toolchain inventory for requirements CAN-MISSION-I0-006 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- `WP-I0-006` — Build-output isolation decision: Define where later authorized build and test outputs may be written without creating a repository copy during the read-only provenance baseline.
- `WP-I0-007` — Baseline build and test attempts: Implement the reviewed Baseline build and test attempts for requirements CAN-MISSION-I0-007 covering 1 requirements: Attempt baseline builds and tests in the disposable workspac.
- `WP-I0-008` — Missing fixture classification: Implement the reviewed Missing fixture classification for requirements CAN-MISSION-I0-008 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- `WP-I0-009` — Frontend coupling inventory: Implement the reviewed Frontend coupling inventory for requirements CAN-MISSION-I0-009 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- `WP-I0-010` — Outbound and commercial integration inventory: Implement the reviewed Outbound and commercial integration inventory for requirements CAN-MISSION-I0-010 covering 1 requirements: Inventory outbound hosts, telemetry, update checks, cloud se.
- `WP-I0-011` — Active-plan authority adoption: Enforce planning authority: canonical requirement IDs, evidence gates, completion-tracker revisions, no-stub completion, and double-check/bottom-up audit obligations before any package advances.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
