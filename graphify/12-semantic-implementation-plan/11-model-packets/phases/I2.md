# I2 — Desktop Shell and Trust Boundary

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Bootstrap the static Tauri desktop shell, capabilities, typed IPC envelope, and offline launch path.
- Entry gate: Required toolchain decisions from I0 are recorded.
- Exit gate: The bounded shell launches offline and rejects unauthorized requests.

## Reviewed work packages

- `WP-I2-001` — Tauri shell bootstrap: Implement and verify the bounded tauri shell bootstrap surface.
- `WP-I2-002` — Static frontend packaging proof: Implement and verify the bounded static frontend packaging proof surface.
- `WP-I2-003` — Offline desktop launch: Implement and verify the bounded offline desktop launch surface.
- `WP-I2-004` — Tauri capabilities and permissions: Implement and verify the bounded tauri capabilities and permissions surface.
- `WP-I2-005` — Local settings bootstrap: Implement and verify the bounded local settings bootstrap surface.
- `WP-I2-007` — IPC envelope and versioning: Implement and verify the bounded ipc envelope and versioning surface.
- `WP-I2-008` — Stable IPC errors: Implement and verify the bounded stable ipc errors surface.
- `WP-I2-011` — Typed frontend command bridge: Implement and verify the bounded typed frontend command bridge surface.
- `WP-I2-013` — Desktop shell smoke proof: Implement and verify the bounded desktop shell smoke proof surface.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
