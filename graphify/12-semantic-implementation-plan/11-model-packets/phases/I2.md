# I2 — Desktop Shell and Trust Boundary

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Bootstrap the static Tauri desktop shell, capabilities, typed IPC envelope, and offline launch path.
- Entry gate: Required toolchain decisions from I0 are recorded.
- Exit gate: The bounded shell launches offline and rejects unauthorized requests.

## Reviewed work packages

- `WP-I2-001` — Tauri shell bootstrap: Establish the Tauri 2 process, typed IPC boundary, permission model, and repository-derived command set before other shell features.
- `WP-I2-002` — Static frontend packaging proof: Implement the reviewed Static frontend packaging proof for requirements  covering 0 requirements: .
- `WP-I2-003` — Offline desktop launch: Implement the reviewed Offline desktop launch for requirements  covering 0 requirements: .
- `WP-I2-004` — Tauri capabilities and permissions: Implement the reviewed Tauri capabilities and permissions for requirements  covering 0 requirements: .
- `WP-I2-005` — Local settings bootstrap: Implement the reviewed Local settings bootstrap for requirements  covering 0 requirements: .
- `WP-I2-007` — IPC envelope and versioning: Implement the reviewed IPC envelope and versioning for requirements CAN-LAM-ARCH-335 covering 1 requirements: UI-to-core calls use Tauri IPC.
- `WP-I2-008` — Stable IPC errors: Implement the reviewed Stable IPC errors for requirements  covering 0 requirements: .
- `WP-I2-011` — Typed frontend command bridge: Implement the reviewed Typed frontend command bridge for requirements CAN-FAIL-03 covering 1 requirements: A completed target screen must obtain mutable or persisted s.
- `WP-I2-013` — Desktop shell smoke proof: Implement the reviewed Desktop shell smoke proof for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01 covering 2 requirements: Lamha must expose library, search, review, map, editing, and; Each managed asset identity must resolve to exactly one auth.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
