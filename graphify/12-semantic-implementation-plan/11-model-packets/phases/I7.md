# I7 — People, Faces, and Groups

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Implement candidate-first face processing, person identity, groups, temporal memberships, and corrections.
- Entry gate: Review contracts and relevant local worker/model decisions exist.
- Exit gate: Candidate, confirmation, merge/split, history, and privacy behavior passes.

## Reviewed work packages

- `WP-I7-001` — Face detection task slice: Run face detection locally, produce reviewable face regions with model/source provenance, support targeted reprocessing, correction scoping, and misclassification review.
- `WP-I7-002` — Face candidate records: Implement and verify the bounded face candidate records surface.
- `WP-I7-003` — Face clustering and curation: Implement and verify the bounded face clustering and curation surface.
- `WP-I7-004` — Person identity model: Persist stable person identities, names, aliases, faces, links, searchable historical/alternate names, and person-match candidates without auto-adding visible people.
- `WP-I7-005` — Person aliases and naming: Implement and verify the bounded person aliases and naming surface.
- `WP-I7-007` — Group identity model: Implement and verify the bounded group identity model surface.
- `WP-I7-008` — Temporal group memberships: Persist complete group membership models with active/former/inactive effective-dated intervals, historical immutability, and overlap validation.
- `WP-I7-009` — Scoped face corrections: Implement and verify the bounded scoped face corrections surface.
- `WP-I7-010` — People and face privacy: Implement and verify the bounded people and face privacy surface.
- `WP-I7-011` — People and groups UI: Implement and verify the bounded people and groups ui surface.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
