# I3 — Authoritative Data and Transaction Foundation

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Implement durable records, roots, sidecars, SQLite migrations, filesystem plans, transactions, and recovery primitives.
- Entry gate: Typed IPC and root-selection boundaries exist.
- Exit gate: Authority, migration, transaction, rollback, and rebuild contracts pass focused tests.

## Reviewed work packages

- `WP-I3-001` — Authoritative record envelope: Implement and verify the bounded authoritative record envelope surface.
- `WP-I3-002` — Stable asset identity: Implement and verify the bounded stable asset identity surface.
- `WP-I3-003` — Root authorization model: Implement and verify the bounded root authorization model surface.
- `WP-I3-004` — Path normalization and escape protection: Implement and verify the bounded path normalization and escape protection surface.
- `WP-I3-005` — Sidecar read protocol: Implement and verify the bounded sidecar read protocol surface.
- `WP-I3-006` — Sidecar write protocol: Write co-located asset JSON and XMP mirrors through a schema-valid sidecar write protocol, preserve exact bytes on validation failure, and keep authoritative knowledge separate from rebuildable indexes.
- `WP-I3-007` — Authoritative schema migrations: Implement and verify the bounded authoritative schema migrations surface.
- `WP-I3-008` — SQLite migration framework: Implement and verify the bounded sqlite migration framework surface.
- `WP-I3-009` — Derived-index rebuild contract: Implement and verify the bounded derived-index rebuild contract surface.
- `WP-I3-010` — Filesystem operation journal: Implement and verify the bounded filesystem operation journal surface.
- `WP-I3-011` — File-operation plan contract: Implement and verify the bounded file-operation plan contract surface.
- `WP-I3-012` — Prepare and stage transaction: Implement and verify the bounded prepare and stage transaction surface.
- `WP-I3-014` — Crash recovery engine: Implement and verify the bounded crash recovery engine surface.
- `WP-I3-015` — Revision and concurrency preconditions: Implement and verify the bounded revision and concurrency preconditions surface.
- `WP-I3-016` — Settings root and drive registries: Implement and verify the bounded settings root and drive registries surface.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
