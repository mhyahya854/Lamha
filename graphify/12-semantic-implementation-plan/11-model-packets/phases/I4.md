# I4 — Media Discovery and Derived Index

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Implement scanning, watching, identity reconciliation, formats, metadata extraction, previews, companions, and indexes.
- Entry gate: I3 authority, root, record, and derived-index foundations exist.
- Exit gate: Supported fixture matrices ingest and rebuild without mutating originals.

## Reviewed work packages

- `WP-I4-001` — Initial recursive scanner: Implement and verify the bounded initial recursive scanner surface.
- `WP-I4-002` — Incremental scan and watcher reconciliation: Implement and verify the bounded incremental scan and watcher reconciliation surface.
- `WP-I4-003` — Media format classification: Implement and verify the bounded media format classification surface.
- `WP-I4-004` — HEIF and RAW decode pipeline: Implement and verify the bounded heif and raw decode pipeline surface.
- `WP-I4-005` — Video probing and playback assets: Implement and verify the bounded video probing and playback assets surface.
- `WP-I4-006` — Companion-media detection: Implement and verify the bounded companion-media detection surface.
- `WP-I4-007` — Content hashing and verification: Implement and verify the bounded content hashing and verification surface.
- `WP-I4-008` — Metadata extraction: Implement and verify the bounded metadata extraction surface.
- `WP-I4-009` — Thumbnail and preview generation: Implement and verify the bounded thumbnail and preview generation surface.
- `WP-I4-010` — Asset and path index: Implement and verify the bounded asset and path index surface.
- `WP-I4-011` — Filesystem reconciliation: Implement and verify the bounded filesystem reconciliation surface.
- `WP-I4-012` — Initial textual index: Implement and verify the bounded initial textual index surface.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
