# I15 — Packaging, Legacy Eradication, and Release

Canonical source: `semantic-plan-source/phases/implementation-phases.json` and the reviewed work-package registry.

- Objective: Package all platforms, remove only verified legacy dependencies, and execute final release proof.
- Entry gate: Required component decisions and replacement evidence are complete.
- Exit gate: Clean-machine packages, SBOM/notices, no-outbound proof, and legacy-removal evidence pass.

## Reviewed work packages

- `WP-I15-001` — Cross-platform packages and clean-machine proof: Implement and verify the bounded windows package and clean-machine proof surface.
- `WP-I15-005` — Signing and notarization pipeline: Implement and verify the bounded signing and notarization pipeline surface.
- `WP-I15-006` — Runtime component licence decisions: Implement and verify the bounded runtime component licence decisions surface.
- `WP-I15-007` — SBOM and third-party notices: Implement and verify the bounded sbom and third-party notices surface.
- `WP-I15-008` — Integrated retained-feature parity: Implement and verify the bounded integrated retained-feature parity surface.
- `WP-I15-014` — Final outbound-traffic verification: Implement and verify the bounded final outbound-traffic verification surface.
- `WP-I15-015` — Legacy replacement verification and removal: Remove legacy runtime, server, database, queue, container, generated-client, and obsolete workflow paths only after retained callers have verified replacements.

This packet is an index. Execute one work-package packet at a time and respect the dependency graph.
