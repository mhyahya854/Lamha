# WP-I6-010 — Event and Manage Later UI

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I6`
- Objective: Implement and verify the bounded event and manage later ui surface.
- Bounded surface: Event and Manage Later UI
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (11)

- `CAN-LAM-ARCH-050` — When Multi-select is requested in 4. Manage Later, Lamha must produce or update a reviewable event plan, show its membership and filesystem effects, and make no authoritative or filesystem change if validation or confirmation fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 584-584)
- `CAN-LAM-ARCH-152` — When a configured library encounters Heading during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1132-1132)
- `CAN-LAM-ARCH-154` — When a configured library encounters Review count during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1140-1140)
- `CAN-LAM-ARCH-155` — When a configured library encounters Offline state during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1141-1141)
- `CAN-LAM-ARCH-156` — When a configured library encounters Read-only state during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1142-1142)
- `CAN-LAM-ASSET-036` — When the user uses Photos with no people in 2.3 Event-first physical organization, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 354-354)
- `CAN-LAM-ASSET-071` — When the user invokes Asset count in 14.5 Folder blocks, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1136-1136)
- `CAN-LAM-FOLDER-029` — When a configured library encounters Open-folder action during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1133-1133)
- `CAN-LAM-FOLDER-030` — When a configured library encounters Physical path during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1134-1134)
- `CAN-LAM-PERSON-020` — When the 2.3 Event-first physical organization workflow manages Unknown people, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 352-352)
- `CAN-LAM-PERSON-063` — When the 14.5 Folder blocks workflow manages People count, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1139-1139)

## Technical prerequisites

- `WP-I6-002` via `REQUIRES_COMPONENT` — Event and Manage Later UI requires authoritative event records and typed commands.

## Direct dependents

`WP-I15-008`

## Contracts and schemas

- IPC commands: `events.create`, `events.plan_link`, `events.plan_merge`, `events.plan_normalize`, `events.plan_split`, `events.update`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for event and manage later ui; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for event and manage later ui; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The event and manage later ui objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
