# WP-I5-011 — Saved views and filters

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I5`
- Objective: Implement the reviewed Saved views and filters for requirements CAN-LAM-ASSET-032 covering 1 requirements: These views must reference the same asset.
- Bounded surface: Saved views and filters
- Explicit exclusions: Excludes behaviors outside Saved views and filters for requirements CAN-LAM-ASSET-032; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-LAM-ASSET-032` — Every view of an asset (gallery, viewer, relationship, and folder views) must reference the same canonical asset identity so changes in one view are reflected in all views. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 327-327)

## Technical prerequisites

- `WP-I4-012` via `REQUIRES_DERIVED_INDEX` — Saved views require the local text/filter index and an explicit authoritative saved-view schema.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-LAM-ASSET-032; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-LAM-ASSET-032; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-LAM-ASSET-032; existing Codebase evidence: Codebase/server/src/repositories/asset-job.repository.ts; Codebase/web/src/lib/components/asset-viewer/PhotoViewer.svelte; Codebase/web/src/lib/components/asset-viewer/VideoNativeViewer.svelte; Codebase/web/src/lib/components/asset-viewer/VideoRemoteViewer.svelte.
- Tests: Run reviewed fixtures for requirements CAN-LAM-ASSET-032; verification methods include Multi-view fixture with identity checks..
- Failure cases: Failures must preserve authoritative state for requirements CAN-LAM-ASSET-032; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-LAM-ASSET-032 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
