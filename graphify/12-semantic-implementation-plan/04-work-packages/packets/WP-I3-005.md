# WP-I3-005 — Sidecar read protocol

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement the reviewed Sidecar read protocol for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045 covering 2 requirements: Clearing selected fields keeps the minimal versioned identit; When Lamha reads or writes an XMP mirror, it must preserve t.
- Bounded surface: Sidecar read protocol
- Explicit exclusions: Excludes behaviors outside Sidecar read protocol for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-LAM-ASSET-134` — Clearing selected fields keeps the minimal versioned identity sidecar required for a managed asset. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1926-1926)
- `CAN-LAM-META-045` — When Lamha reads or writes an XMP mirror, it must preserve the authoritative value and provenance, validate the mirror revision, and surface missing, malformed, stale, or conflicting XMP as a typed degraded or review state without discarding the last valid authority. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2002-2002)

## Technical prerequisites

- `WP-I3-001` via `REQUIRES_SCHEMA` — Sidecar parsing targets a versioned record envelope.

## Direct dependents

`WP-I3-006`, `WP-I4-008`, `WP-I5-016`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; existing Codebase evidence: Codebase/mobile/lib/infrastructure/entities/remote_asset_cloud_id.entity.dart; Codebase/mobile/lib/infrastructure/entities/remote_asset_cloud_id.entity.drift.dart; Codebase/server/src/controllers/asset.controller.ts; Codebase/server/src/repositories/metadata.repository.ts; Codebase/server/src/schema/migrations/1764698859174-SidecarInAssetFile.ts; Codebase/server/src/services/metadata.service.ts; Codebase/web/src/lib/components/asset-viewer/DetailPanelDate.svelte; Codebase/web/src/lib/managers/asset-multi-select-manager.svelte.ts.
- Tests: Run reviewed fixtures for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; verification methods include Gate 1 scope; Gate 2 compile/build; Gate 3 focused/regressio; Given valid JSON authority and malformed or conflicting XMP,.
- Failure cases: Failures must preserve authoritative state for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-LAM-ASSET-134, CAN-LAM-META-045 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
