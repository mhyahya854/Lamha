# WP-I3-005 — Sidecar read protocol

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement and verify the bounded sidecar read protocol surface.
- Bounded surface: Sidecar read protocol
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-LAM-ASSET-134` — Clearing selected fields keeps the minimal versioned identity sidecar required for a managed asset. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1926-1926)
- `CAN-LAM-META-045` — When Lamha reads or writes an XMP mirror, it must preserve the authoritative value and provenance, validate the mirror revision, and surface missing, malformed, stale, or conflicting XMP as a typed degraded or review state without discarding the last valid authority. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2002-2002)

## Technical prerequisites

- `WP-I3-001` via `REQUIRES_SCHEMA` — Sidecar parsing targets a versioned record envelope.

## Direct dependents

`WP-I3-006`, `WP-I4-008`, `WP-I5-016`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for sidecar read protocol; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for sidecar read protocol; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The sidecar read protocol objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
