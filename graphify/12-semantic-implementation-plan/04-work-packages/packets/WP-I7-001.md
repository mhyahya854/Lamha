# WP-I7-001 — Face detection task slice

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I7`
- Objective: Implement and verify the bounded face detection task slice surface.
- Bounded surface: Face detection task slice
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (5)

- `CAN-LAM-AI-022` — Correcting a face must update only affected assets and derived metadata unless the underlying model or source file changed. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 881-881)
- `CAN-LAM-AI-023` — When the 12. Groups and Subgroups workflow manages Lamha keeps a complete group model, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 922-922)
- `CAN-LAM-ARCH-105` — When the 10.2 Face corrections workflow manages Targeted reprocessing, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 878-878)
- `CAN-LAM-PERSON-007` — When the 2.1 Local-first operation workflow manages Face detection, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 283-283)
- `CAN-LAM-PERSON-128` — When the 26. Risk Management workflow manages Face misclassification, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1732-1732)

## Technical prerequisites

- `WP-I10-001` via `REQUIRES_WORKER` — Detection runs only through the owned local worker lifecycle.
- `WP-I10-003` via `REQUIRES_COMPONENT_DECISION` — Detection requires a reviewed model/runtime record.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: `people.merge`, `people.split`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for face detection task slice; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for face detection task slice; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The face detection task slice objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
