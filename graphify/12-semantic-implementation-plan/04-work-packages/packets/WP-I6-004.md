# WP-I6-004 — Event inference proposals

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I6`
- Objective: Implement and verify the bounded event inference proposals surface.
- Bounded surface: Event inference proposals
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-LAM-FOLDER-001` — When a user approves event materialization in a writable Library Root, Lamha must create exactly one dated Year/YYYY-MM-DD-Event-Name folder or one Unknown Date/Unknown-Date-Event-Name folder and persist the approved naming inputs. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 65-65)
- `CAN-LAM-PERSON-021` — When the 2.3 Event-first physical organization workflow manages Mixed groups, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 353-353)

## Technical prerequisites

- `WP-I4-010` via `REQUIRES_INDEX` — Inference queries indexed timestamps and locations.
- `WP-I6-002` via `REQUIRES_SCHEMA` — Inference produces reviewable event drafts.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for event inference proposals; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for event inference proposals; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The event inference proposals objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
