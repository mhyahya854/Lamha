# WP-I2-013 — Desktop shell smoke proof

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I2`
- Objective: Implement the reviewed Desktop shell smoke proof for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01 covering 2 requirements: Lamha must expose library, search, review, map, editing, and; Each managed asset identity must resolve to exactly one auth.
- Bounded surface: Desktop shell smoke proof
- Explicit exclusions: Excludes behaviors outside Desktop shell smoke proof for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-LAM-ARCH-014` — Lamha must expose library, search, review, map, editing, and maintenance workflows through one desktop shell with shared navigation, identity, settings, error presentation, and operation status. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 41-41)
- `CAN-LAM-INV-ASSET-01` — Each managed asset identity must resolve to exactly one authoritative primary media file; categorization, albums, tags, events, and saved views must reference that identity without duplicating the media container. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 93-93)

## Technical prerequisites

- `WP-I2-003` via `REQUIRES_PLATFORM_PROOF` — Shell smoke proof exercises the packaged local launch path.

## Direct dependents

`WP-I15-001`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; existing Codebase evidence: Codebase/mobile/ios/Runner/AppDelegate.swift; Codebase/mobile/openapi/lib/api/assets_api.dart; Codebase/package.json; Codebase/package.json); Codebase/packages/cli/package.json; Codebase/server/package.json; Codebase/server/src/dtos/asset-media-response.dto.ts; Codebase/server/src/dtos/asset-media.dto.ts.
- Tests: Run reviewed fixtures for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; verification methods include Given each primary workflow, when a user opens it and starts; Given one asset referenced by multiple organizational concep.
- Failure cases: Failures must preserve authoritative state for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-LAM-ARCH-014, CAN-LAM-INV-ASSET-01 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
