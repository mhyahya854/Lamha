# WP-I0-002 — Read-only Git-state inspection

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Inspect existing Git state without creating or modifying commits, branches, tags, stashes, worktrees, the index, or configuration.
- Bounded surface: Existing Git-state provenance inspection
- Explicit exclusions: Excludes behaviors outside Existing Git-state provenance inspection for requirements CAN-MISSION-I0-002; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-002` — During I0, the implementation team may inspect existing Git metadata with optional locks disabled, but it must not create or modify commits, branches, tags, stashes, worktrees, the index, or repository configuration. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-001` via `REQUIRES_PROVENANCE` — Git-state inspection uses the resolved repository identity and read-only boundary established by the provenance baseline.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-002; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-002; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Graphify-only report of existing Git state and before/after metadata integrity evidence.
- Tests: Run read-only Git inspection with optional locks disabled and prove relevant Git metadata is unchanged.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-002; invalid input returns typed errors and no partial commit.
- Rollback/recovery: Not applicable because Git mutation is prohibited.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Existing Git state is recorded and before/after metadata integrity is identical; no Git mutation occurred.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
