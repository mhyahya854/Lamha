# WP-I11-001 — Authority-aware metadata editor

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I11`
- Objective: Implement and verify the bounded authority-aware metadata editor surface.
- Bounded surface: Authority-aware metadata editor
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-FAIL-28` — When authoritative records, embedded metadata, XMP mirrors, or pending overlays disagree, Lamha must preserve every source value and provenance, keep the last valid authority unchanged, and create a typed review conflict instead of silently choosing a winner. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 125-125)
- `CAN-LAM-META-071` — When the Phase 11 — Metadata Mutation, Editing & Privacy workflow reads or changes Embedded metadata removal, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1498-1498)

## Technical prerequisites

- `WP-I3-006` via `REQUIRES_STORAGE` — Metadata mutation persists via the safe sidecar protocol.
- `WP-I3-011` via `REQUIRES_CONTRACT` — Mutating batches require previewed operation plans.

## Direct dependents

`WP-I11-002`, `WP-I11-003`

## Contracts and schemas

- IPC commands: `editing.export_commit`, `editing.export_plan`, `editing.recipe_get`, `editing.recipe_update`, `editing.render_preview`, `editing.reset`, `metadata.apply_update`, `metadata.inspect`, `metadata.propose_update`, `metadata.restore`, `metadata.snapshot`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for authority-aware metadata editor; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for authority-aware metadata editor; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The authority-aware metadata editor objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
