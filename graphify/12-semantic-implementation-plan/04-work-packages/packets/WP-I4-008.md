# WP-I4-008 — Metadata extraction

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I4`
- Objective: Implement and verify the bounded metadata extraction surface.
- Bounded surface: Metadata extraction
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (11)

- `CAN-LAM-EDGE-03` — When the 1.5 Operational Edge-Case Registry for Files, Sidecars, Dates, and Metadata workflow reads or changes LAM-EDGE-03; Sidecars; External File Renaming or Moving; Scanner finds a new path with an exact stable UUID or full SHA-256 match.; Update the derived path index. If a deterministic, same-root, writable reunion is possible, transact the sidecars to match and log it; otherwise create Review.; Do NOT auto-match on filename/partial hash alone, cross an unauthorized boundary, create a duplicate UUID, or delete metadata.; Reunite or relink only after identity and destination checks; ambiguous candidates remain unresolved, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 250-250)
- `CAN-LAM-INV-META-01` — For current path, directory, and filename fields, Lamha must read the operating-system filesystem as authority and treat cached SQLite or sidecar values only as projections that reconciliation may update. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 106-106)
- `CAN-LAM-META-013` — When the 2.1 Local-first operation workflow reads or changes Reading metadata, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 279-279)
- `CAN-LAM-META-014` — When the 2.1 Local-first operation workflow reads or changes Editing supported metadata, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 280-280)
- `CAN-LAM-META-026` — When the 8. Local Library Scanning workflow reads or changes Metadata extraction, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 712-712)
- `CAN-LAM-META-033` — When the 21. Search workflow reads or changes Missing metadata, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1593-1593)
- `CAN-LAM-META-036` — When the 24. Map and Location workflow reads or changes EXIF GPS reading, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1745-1745)
- `CAN-LAM-META-040` — When the 29. Consistency Detection workflow reads or changes JSON versus XMP, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1895-1895)
- `CAN-LAM-META-041` — When the 29. Consistency Detection workflow reads or changes JSON versus embedded metadata, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1896-1896)
- `CAN-LAM-META-043` — When the 29. Consistency Detection workflow reads or changes Missing XMP, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1901-1901)
- `CAN-LAM-META-049` — When the 36. Target Local Architecture We Are Keeping workflow reads or changes Useful metadata UI, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2158-2158)

## Technical prerequisites

- `WP-I3-005` via `REQUIRES_SCHEMA` — Extraction precedence must account for durable sidecars.
- `WP-I4-003` via `REQUIRES_COMPONENT_DECISION` — Metadata extraction depends on format-aware reviewed tooling.

## Direct dependents

`WP-I5-008`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for metadata extraction; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for metadata extraction; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The metadata extraction objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
