# WP-I3-008 — SQLite migration framework

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement and verify the bounded sqlite migration framework surface.
- Bounded surface: SQLite migration framework
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (9)

- `CAN-LAM-ASSET-018` — Favorite state; Asset JSON; Compatible XMP rating field; SQLite favorite index; Asset favorite state is JSON-authoritative. External XMP rating changes enter Review before JSON changes. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 144-144)
- `CAN-LAM-ASSET-024` — Album definition; Album JSON; Asset UUID membership references; SQLite index; Album title, cover, and ordering live in Album JSON; per-asset membership mirrors may accelerate rebuild but do not compete. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 150-150)
- `CAN-LAM-EDGE-04` — When an event builder or move command targets a destination whose filename already exists with a different hash, Lamha must append a deterministic sequence suffix before the extension, preserve the original filename in the sidecar, and never overwrite the existing file or merge sidecars of different assets. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 251-251)
- `CAN-LAM-EDGE-10` — When startup finds an uncommitted local coordinator or root-scoped manifest after a crash, Lamha must reconcile copies by transaction UUID, verify source/destination hashes, and auto-finalize or roll back only when deterministic; otherwise it must open Review and persist the recovery outcome. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 257-257)
- `CAN-LAM-GOV-224` — Saved user decisions, saved map drafts, rejection/suppression decisions, durable operation history, and unresolved Pending Overlays must also exist in transparent JSON authority and survive SQLite loss. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 956-956)
- `CAN-LAM-GOV-256` — SQLite may index transaction state but is never the sole recovery manifest. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1086-1086)
- `CAN-LAM-META-002` — Each retained feature that persists user knowledge must write its reviewed authoritative record, update or rebuild its SQLite projection, and return typed results for success, invalid input, I/O failure, conflict, cancellation, and recovery. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 51-51)
- `CAN-LAM-META-012` — Pending overlay; Versioned overlay JSON while destination is unavailable; Target authoritative JSON after reconciliation; SQLite overlay index; Flush only after destination identity, source version, and conflicts are checked; keep the overlay until commit proof exists. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 152-152)
- `CAN-LAM-META-046` — SQLite must remain a rebuildable index and must never be the only copy of user knowledge. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2022-2022)

## Technical prerequisites

- `WP-I3-001` via `REQUIRES_SCHEMA` — Derived tables must trace to authoritative record fields.

## Direct dependents

`WP-I3-009`, `WP-I4-001`, `WP-I4-010`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for sqlite migration framework; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for sqlite migration framework; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The sqlite migration framework objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
