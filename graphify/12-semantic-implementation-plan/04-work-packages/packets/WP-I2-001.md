# WP-I2-001 — Tauri shell bootstrap

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I2`
- Objective: Establish the Tauri 2 process, typed IPC boundary, permission model, and repository-derived command set before other shell features.
- Bounded surface: Tauri 2 shell bootstrap, IPC boundary, and command-set authority
- Explicit exclusions: IPC envelope versioning/errors; frontend packaging; offline launch.
- Commit boundary: Shell bootstrap and IPC boundary only.

## Canonical requirements (2)

- `CAN-LAM-ARCH-370` — The desktop shell must establish the reviewed Tauri 2 process, typed IPC boundary, and permission model before other shell features. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1377-1377)
- `CAN-LAM-GOV-218` — The exact command set, payload types, and ownership must be derived from repository evidence and the three Master Plans. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 893-893)

## Technical prerequisites

- `WP-I0-005` via `REQUIRES_RUNTIME` — The Rust/Tauri shell cannot be selected or compiled before toolchain proof.
- `WP-I0-005` via `REQUIRES_COMPONENT_DECISION` — WP-I2-001 consumes the finalized Rust toolchain decision owned by WP-I0-005.

## Direct dependents

`WP-I14-013`, `WP-I2-003`, `WP-I2-003`, `WP-I2-004`, `WP-I2-004`, `WP-I2-007`, `WP-I2-007`, `WP-I2-008`, `WP-I2-011`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Tauri shell bootstrap and IPC boundary contracts
- Schemas affected: Shell bootstrap and command-authority records

## Delivery and proof

- Deliverables: Tauri 2 process bootstrap, typed IPC boundary, permission model, and command-set authority records.
- Tests: Fixture suite asserting shell bootstraps offline, IPC boundary rejects unauthorized calls, and command set matches repository evidence.
- Failure cases: Tauri process failure, unauthorized IPC call, command-set mismatch.
- Rollback/recovery: Bootstrap is staged; failed boot returns a typed error without partial trust state.
- Completion evidence: All shell-bootstrap fixtures pass with repository-derived command set.
- Exit gate: Tauri shell bootstrap suite passes.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
