# WP-I5-005 — Selection and keyboard navigation

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I5`
- Objective: Implement and verify the bounded selection and keyboard navigation surface.
- Bounded surface: Selection and keyboard navigation
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (5)

- `CAN-LAM-ARCH-065` — When the user uses Multi-selection in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 655-655)
- `CAN-LAM-ARCH-066` — When the user uses Keyboard navigation in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 656-656)
- `CAN-LAM-ARCH-071` — When the user invokes Previous and next navigation in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 678-678)
- `CAN-LAM-ARCH-239` — When Same month in previous years is shown in Memories, Lamha must derive the memory from local canonical asset references, show a reproducible result, and omit unavailable assets without changing their records. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1727-1727)
- `CAN-LAM-ARCH-277` — When a review item offers Apply to selection in 27. Review Centre, the selected decision must produce a visible, persisted proposal-state transition while leaving authoritative metadata unchanged until an allowed approval action commits it; rejected invalid transitions must preserve the prior state. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1846-1846)

## Technical prerequisites

- `WP-I5-001` via `REQUIRES_COMPONENT` — Selection and keyboard navigation operate on the virtualized gallery item and focus model.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for selection and keyboard navigation; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for selection and keyboard navigation; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The selection and keyboard navigation objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
