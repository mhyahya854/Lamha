# WP-I3-010 — Filesystem operation journal

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement and verify the bounded filesystem operation journal surface.
- Bounded surface: Filesystem operation journal
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (4)

- `CAN-LAM-ASSET-125` — When the user invokes Move asset bundle in 28. File and Folder Management, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1865-1865)
- `CAN-LAM-GOV-257` — When a configured library encounters Operations include during 17. Filesystem Transaction Method, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1088-1088)
- `CAN-LAM-TRANSACTION-001` — LAM-TRANSACTION-001; Authoritative filesystem mutations use a transparent local coordinator manifest plus affected-root mirrors keyed by one transaction UUID; SQLite is index only; the complete bundle is preserved. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 656-656)
- `CAN-LAM-TRANSACTION-002` — When the 2.4 Transparent and recoverable metadata workflow reads or changes Operation journals, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 376-376)

## Technical prerequisites

- `WP-I3-003` via `REQUIRES_FILESYSTEM_AUTHORITY` — The operation journal must validate every target against authorized root identity before recording mutation state.

## Direct dependents

`WP-I3-014`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for filesystem operation journal; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for filesystem operation journal; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The filesystem operation journal objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
