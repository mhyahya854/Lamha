# WP-I11-010 — Metadata snapshots and restore

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I11`
- Objective: Implement and verify the bounded metadata snapshots and restore surface.
- Bounded surface: Metadata snapshots and restore
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (5)

- `CAN-LAM-ARCH-398` — When the Phase 11 — Metadata Mutation, Editing & Privacy workflow reads or changes Snapshots, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1500-1500)
- `CAN-LAM-GOV-018` — Git history, a commit, signed inventory, archive, snapshot, behaviour logs, graph hashes, or test records may preserve the baseline; active obsolete files need not remain solely for ceremony. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 40-40)
- `CAN-LAM-META-019` — When the 2.6 Non-destructive defaults workflow reads or changes Metadata snapshots, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 410-410)
- `CAN-LAM-META-075` — When the 24.4 Safety proofs workflow reads or changes Metadata snapshots are recoverable, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1684-1684)
- `CAN-SECTION-0120` — Metadata mutation must preserve original filename provenance, create revisioned metadata snapshots, and produce privacy-clean exports without modifying the managed original or discarding the last valid authoritative value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / section aggregate)

## Technical prerequisites

- `WP-I3-006` via `REQUIRES_STORAGE` — Metadata snapshots and restore require the transactional sidecar write protocol.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for metadata snapshots and restore; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for metadata snapshots and restore; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The metadata snapshots and restore objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
