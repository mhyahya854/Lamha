# WP-I7-003 — Face clustering and curation

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I7`
- Objective: Implement and verify the bounded face clustering and curation surface.
- Bounded surface: Face clustering and curation
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (10)

- `CAN-FAIL-06` — Face-cluster analysis may create typed merge candidates, but Lamha must not persist a cluster merge until a user or an explicitly reviewed narrow rule approves it through the Review Centre. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 103-103)
- `CAN-LAM-ARCH-103` — When the 10.2 Face corrections workflow manages Split mixed clusters, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 872-872)
- `CAN-LAM-ARCH-382` — When the Phase 7 — Faces, People & Groups workflow manages Clusters, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1435-1435)
- `CAN-LAM-EDGE-07` — When the 1.5 Operational Edge-Case Registry for Files, Sidecars, Dates, and Metadata workflow reads or changes LAM-EDGE-07; People; Accidental Person Merge; User merges face clusters representing different people.; Record a durable pre-merge snapshot and expose Undo in operation history.; Do NOT erase either UUID, rely on a one-second UI window, or overwrite historical face links.; Restore the exact pre-merge person/face/reference state from the durable transaction snapshot, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 254-254)
- `CAN-LAM-PERSON-008` — When the 2.1 Local-first operation workflow manages Face recognition, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 284-284)
- `CAN-LAM-PERSON-009` — When the 2.1 Local-first operation workflow manages Face clustering, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 285-285)
- `CAN-LAM-PERSON-027` — When the 2.5 Review before final acceptance workflow manages Group face clusters, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 388-388)
- `CAN-LAM-PERSON-033` — The required workflow is: Detect faces / → Group similar faces / → Show one cluster / → Enter person name / → Add or confirm any applicable built-in/custom relationship edges / → Set effective dates, certainty, notes, and explicit composition projections where applicable / → Select or create groups / → Confirm matching faces / → Review uncertain matches / → Save person record / → Generate derived tag suggestions / → Send suggestions to Review. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 844-859)
- `CAN-LAM-PERSON-034` — When the 10.2 Face corrections workflow manages Merge same-person clusters, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 871-871)
- `CAN-LAM-PERSON-041` — When the 11. People workflow manages Several face clusters, it must persist the candidate or confirmed state with provenance and revision, expose the resulting state for review, and reject invalid transitions without changing an existing identity or historical interval. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 893-893)

## Technical prerequisites

- `WP-I7-002` via `REQUIRES_SCHEMA` — Clustering operates on persisted candidate observations.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for face clustering and curation; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for face clustering and curation; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The face clustering and curation objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
