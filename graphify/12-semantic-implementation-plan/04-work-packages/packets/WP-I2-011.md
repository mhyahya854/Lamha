# WP-I2-011 — Typed frontend command bridge

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I2`
- Objective: Implement the reviewed Typed frontend command bridge for requirements CAN-FAIL-03 covering 1 requirements: A completed target screen must obtain mutable or persisted s.
- Bounded surface: Typed frontend command bridge
- Explicit exclusions: Excludes behaviors outside Typed frontend command bridge for requirements CAN-FAIL-03; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-FAIL-03` — A completed target screen must obtain mutable or persisted state through its reviewed typed Tauri command and render the returned local data; hard-coded production mock state is prohibited. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 100-100)

## Technical prerequisites

- `WP-I2-001` via `REQUIRES_COMPONENT_DECISION` — WP-I2-011 consumes the finalized Tauri decision owned by WP-I2-001.
- `WP-I2-002` via `REQUIRES_COMPONENT_DECISION` — WP-I2-011 consumes the finalized Svelte/SvelteKit decision owned by WP-I2-002.
- `WP-I2-003` via `REQUIRES_COMPONENT_DECISION` — WP-I2-011 consumes the finalized Platform WebView dependencies decision owned by WP-I2-003.
- `WP-I2-007` via `REQUIRES_CONTRACT` — The frontend bridge is generated from approved IPC contracts.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-FAIL-03; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-FAIL-03; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-FAIL-03; existing Codebase evidence: Codebase/server/test/repositories/metadata.repository.mock.ts; Codebase/server/test/repositories/system-metadata.repository.mock.ts; Codebase/web/src/lib/components/TestWrapper.svelte; Codebase/web/src/test-data/mocks/UserPageLayout.mock.svelte.
- Tests: Run reviewed fixtures for requirements CAN-FAIL-03; verification methods include Live Tauri IPC wiring gate..
- Failure cases: Failures must preserve authoritative state for requirements CAN-FAIL-03; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-FAIL-03 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
