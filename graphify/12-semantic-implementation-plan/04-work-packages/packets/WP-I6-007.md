# WP-I6-007 — Event naming and normalization

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I6`
- Objective: Implement and verify the bounded event naming and normalization surface.
- Bounded surface: Event naming and normalization
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (6)

- `CAN-LAM-ARCH-051` — When Bulk rename preview is requested in 4. Manage Later, Lamha must produce or update a reviewable event plan, show its membership and filesystem effects, and make no authoritative or filesystem change if validation or confirmation fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 586-586)
- `CAN-LAM-EDGE-16` — When the 1.5 Operational Edge-Case Registry for Files, Sidecars, Dates, and Metadata workflow reads or changes LAM-EDGE-16; Dates; Timestamp Missing Offset or Time Zone; Embedded timestamp has no reliable offset/time-zone provenance or sources disagree.; Preserve the raw source value and mark normalized time/zone uncertainty.; Do NOT silently assume UTC, local time, event time, or a folder date.; Show evidence in Review; a user-approved normalized value updates JSON while retaining the raw snapshot, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 263-263)
- `CAN-LAM-EVENT-007` — When a configured library encounters Normalized managed Event Folders during 3. Final Physical Media Library, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 500-500)
- `CAN-LAM-EVENT-046` — When a configured library encounters Event name during 14.5 Folder blocks, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1138-1138)
- `CAN-LAM-FOLDER-068` — When Folder normalization is requested in Phase 6 — Manage Later & Events, Lamha must produce or update a reviewable event plan, show its membership and filesystem effects, and make no authoritative or filesystem change if validation or confirmation fails. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1425-1425)
- `CAN-LAM-INV-EVENT-01` — When Lamha materializes an approved event, it must create the typed dated or Unknown Date directory name from approved event fields and must never silently promote a capture-time suggestion into the authoritative date. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 100-100)

## Technical prerequisites

- `WP-I6-002` via `REQUIRES_SCHEMA` — Event naming and normalization use authoritative event dates and stable identity.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for event naming and normalization; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for event naming and normalization; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The event naming and normalization objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
