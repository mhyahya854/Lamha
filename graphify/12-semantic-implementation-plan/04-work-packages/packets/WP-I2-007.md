# WP-I2-007 — IPC envelope and versioning

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I2`
- Objective: Implement the reviewed IPC envelope and versioning for requirements CAN-LAM-ARCH-335 covering 1 requirements: UI-to-core calls use Tauri IPC.
- Bounded surface: IPC envelope and versioning
- Explicit exclusions: Excludes behaviors outside IPC envelope and versioning for requirements CAN-LAM-ARCH-335; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-LAM-ARCH-335` — Every UI-to-core call must use the reviewed typed Tauri IPC command and render returned local data; hard-coded production mock state is prohibited. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2163-2163)

## Technical prerequisites

- `WP-I2-001` via `REQUIRES_RUNTIME` — Typed commands require the Rust command host.
- `WP-I2-001` via `REQUIRES_COMPONENT_DECISION` — WP-I2-007 consumes the finalized Tauri decision owned by WP-I2-001.

## Direct dependents

`WP-I10-001`, `WP-I2-005`, `WP-I2-008`, `WP-I2-011`, `WP-I3-001`, `WP-I5-010`

## Contracts and schemas

- IPC commands: `app.bootstrap`, `app.diagnostics_export`, `app.settings_get`, `app.settings_update`, `app.status`
- Contracts affected: Contracts affected by requirements CAN-LAM-ARCH-335; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-LAM-ARCH-335; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-LAM-ARCH-335; existing Codebase evidence: Codebase/package.json; Codebase/package.json); Codebase/packages/cli/package.json; Codebase/server/package.json; Codebase/web/package.json.
- Tests: Run reviewed fixtures for requirements CAN-LAM-ARCH-335; verification methods include UI-to-core IPC fixture..
- Failure cases: Failures must preserve authoritative state for requirements CAN-LAM-ARCH-335; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-LAM-ARCH-335 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
