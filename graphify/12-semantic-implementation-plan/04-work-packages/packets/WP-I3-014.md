# WP-I3-014 — Crash recovery engine

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement and verify the bounded crash recovery engine surface.
- Bounded surface: Crash recovery engine
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-FAIL-20` — Before a multi-file mutation, Lamha must durably prepare a transaction manifest, stage and verify outputs, commit atomically where possible, and recover or roll back by transaction identity after crash or disconnection. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 117-117)
- `CAN-LAM-ARCH-282` — When the 28. File and Folder Management workflow reads or changes Sidecar repair, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1880-1880)

## Technical prerequisites

- `WP-I3-010` via `REQUIRES_TRANSACTION_ENGINE` — Crash recovery replays or rolls back durable operation-journal states by transaction identity.

## Direct dependents

`WP-I11-012`, `WP-I12-007`, `WP-I13-008`

## Contracts and schemas

- IPC commands: `operations.recover`, `operations.rollback`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for crash recovery engine; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for crash recovery engine; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The crash recovery engine objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
