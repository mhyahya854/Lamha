# WP-I3-006 — Sidecar write protocol

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Write co-located asset JSON and XMP mirrors through a schema-valid sidecar write protocol, preserve exact bytes on validation failure, and keep authoritative knowledge separate from rebuildable indexes.
- Bounded surface: Sidecar write protocol and authoritative sidecar separation
- Explicit exclusions: Sidecar read protocol; schema migrations; SQLite index rebuild.
- Commit boundary: Sidecar write protocol only.

## Canonical requirements (3)

- `CAN-FAIL-04` — For every writable managed asset whose approved metadata is XMP-compatible, Lamha must write the co-located asset record and XMP mirror or expose a typed degraded state until reconciliation succeeds; silent XMP omission is prohibited. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 101-101)
- `CAN-LAM-ARCH-425` — Sidecar separation must keep authoritative user knowledge in versioned files separate from rebuildable SQLite indexes and caches. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1727-1727)
- `CAN-LAM-ASSET-138` — Asset JSON must be read and written as the schema-valid authoritative record with exact-byte preservation on validation failure. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2001-2001)

## Technical prerequisites

- `WP-I3-005` via `REQUIRES_SCHEMA` — Safe writes preserve and round-trip fields understood by the reader.

## Direct dependents

`WP-I11-001`, `WP-I11-010`, `WP-I5-017`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Sidecar write protocol contracts
- Schemas affected: Asset and XMP sidecar records

## Delivery and proof

- Deliverables: Sidecar write commands, asset JSON/XMP mirror writes, exact-byte preservation on validation failure, and sidecar separation rules.
- Tests: Fixture suite covering JSON/XMP writes, schema validation failure preservation, mirror alignment, and index separation.
- Failure cases: Schema-invalid writes, mirror mismatch, write interruption.
- Rollback/recovery: Writes are journaled; failed validation preserves the prior sidecar bytes.
- Completion evidence: All sidecar-write fixtures pass with exact-byte preservation.
- Exit gate: Sidecar write fixture suite passes.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
