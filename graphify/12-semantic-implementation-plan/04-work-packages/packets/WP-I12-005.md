# WP-I12-005 — Pending sidecar overlays

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I12`
- Objective: Implement the reviewed Pending sidecar overlays for requirements CAN-FAIL-16, CAN-LAM-ARCH-020 covering 2 requirements: When a root is read-only, Lamha must record the proposed cha; A pending sidecar overlay must persist until it is applied o.
- Bounded surface: Pending sidecar overlays
- Explicit exclusions: Excludes behaviors outside Pending sidecar overlays for requirements CAN-FAIL-16, CAN-LAM-ARCH-020; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-FAIL-16` — When a root is read-only, Lamha must record the proposed change as a versioned pending overlay in application data, report its pending state, and reconcile only after root identity, revision, and conflict checks pass. (source: 02-EVERYTHING-WE-ARE-DELETING.md / lines 113-113)
- `CAN-LAM-ARCH-020` — A pending sidecar overlay must persist until it is applied or explicitly rejected, remain visible in Review, and pass revision-conflict validation before any value is flushed to authoritative metadata. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 81-81)

## Technical prerequisites

- `WP-I12-004` via `REQUIRES_STORAGE` — Pending sidecar overlays are the durable write path exposed by detached read-only mode.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-FAIL-16, CAN-LAM-ARCH-020; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-FAIL-16, CAN-LAM-ARCH-020; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-FAIL-16, CAN-LAM-ARCH-020; existing Codebase evidence: Codebase/mobile/lib/domain/models/user_metadata.model.dart; Codebase/mobile/lib/models/server_info/server_version.model.dart; Codebase/mobile/openapi/lib/model/storage_folder.dart; Codebase/mobile/openapi/lib/model/system_config_template_storage_option_dto.dart; Codebase/server/src/repositories/workflow.repository.ts; Codebase/server/src/services/workflow.service.ts; Codebase/web/src/lib/components/asset-viewer/PhotoSphereViewerAdapter.svelte.
- Tests: Run reviewed fixtures for requirements CAN-FAIL-16, CAN-LAM-ARCH-020; verification methods include Read-only overlay and reconciliation gate.; Given a pending overlay, when the app restarts, a conflict a.
- Failure cases: Failures must preserve authoritative state for requirements CAN-FAIL-16, CAN-LAM-ARCH-020; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-FAIL-16, CAN-LAM-ARCH-020 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
