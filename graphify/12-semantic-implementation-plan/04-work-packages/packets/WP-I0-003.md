# WP-I0-003 — Existing repository SHA-256 manifest

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Calculate and verify path, size, and SHA-256 evidence for existing repository files without creating an archive, backup, or duplicate tree.
- Bounded surface: Existing-file integrity manifest
- Explicit exclusions: Excludes behaviors outside Existing-file integrity manifest for requirements CAN-MISSION-I0-003; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-003` — During I0, the implementation team must calculate and verify a SHA-256 manifest for existing repository files without creating an archive, backup, or duplicate repository tree. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-001` via `REQUIRES_PROVENANCE` — The repository hash manifest requires the resolved roots and exclusion boundary from the provenance baseline.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-003; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-003; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Graphify-only existing-file SHA-256 manifest and verification report.
- Tests: Rehash every manifest entry and verify path, byte size, and digest equality.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-003; invalid input returns typed errors and no partial commit.
- Rollback/recovery: Not applicable because source files remain read-only.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Every manifest entry verifies and no archive, backup, copy, or repository mutation exists.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
