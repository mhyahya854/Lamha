# WP-I14-008 — Map and graph accessibility

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I14`
- Objective: Implement and verify the bounded map and graph accessibility surface.
- Bounded surface: Map and graph accessibility
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (2)

- `CAN-LAM-ARCH-408` — When the desktop shell handles Graph virtualization in Phase 14 — Performance, Accessibility & Desktop UX, it must use the typed Tauri boundary, expose the result or stable error to the frontend, and reject requests outside the granted capability without side effects. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1538-1538)
- `CAN-MISSION-I14-002` — Each graph, map, timeline, and review interface must expose keyboard navigation, visible focus, semantic labels, screen-reader output, and a non-visual alternative that provides the same supported actions and state. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I14-005` via `REQUIRES_COMPONENT` — Map and graph accessibility applies the shared accessibility semantics and input model.
- `WP-I9-001` via `REQUIRES_GRAPH_MODEL` — Accessible map navigation requires stable graph nodes, edges, and focus identity.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for map and graph accessibility; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for map and graph accessibility; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The map and graph accessibility objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
