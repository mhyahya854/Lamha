# WP-I0-009 — Frontend coupling inventory

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Implement the reviewed Frontend coupling inventory for requirements CAN-MISSION-I0-009 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- Bounded surface: Frontend coupling inventory
- Explicit exclusions: Excludes behaviors outside Frontend coupling inventory for requirements CAN-MISSION-I0-009; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-009` — During read-only I0 inspection, the implementation team must record each frontend dependency on server routes, generated clients, authentication, runtime hosts, or server-owned state and link it to the owning replacement decision or package. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-001` via `REQUIRES_PROVENANCE` — Frontend coupling inventory requires the resolved read-only repository corpus and exact current paths.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-009; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-009; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-MISSION-I0-009; existing Codebase evidence: none recorded.
- Tests: Run reviewed fixtures for requirements CAN-MISSION-I0-009; verification methods include Given the frontend source, when coupling inspection runs, th.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-009; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-MISSION-I0-009 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
