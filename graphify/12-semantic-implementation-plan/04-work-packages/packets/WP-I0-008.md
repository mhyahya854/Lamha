# WP-I0-008 — Missing fixture classification

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Implement the reviewed Missing fixture classification for requirements CAN-MISSION-I0-008 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- Bounded surface: Missing fixture classification
- Explicit exclusions: Excludes behaviors outside Missing fixture classification for requirements CAN-MISSION-I0-008; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-008` — During read-only I0 inspection, the implementation team must classify each observed missing fixture, generated artifact, dependency, credential, or environment prerequisite and record its evidence and blocking effect without generating, installing, or supplying it. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-001` via `REQUIRES_PROVENANCE` — Missing-fixture classification requires the complete existing-file inventory to distinguish absence from an excluded path.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-008; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-008; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-MISSION-I0-008; existing Codebase evidence: none recorded.
- Tests: Run reviewed fixtures for requirements CAN-MISSION-I0-008; verification methods include Given a missing prerequisite, when classification runs, then.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-008; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-MISSION-I0-008 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
