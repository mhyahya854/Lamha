# WP-I2-004 — Tauri capabilities and permissions

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I2`
- Objective: Implement the reviewed Tauri capabilities and permissions for requirements  covering 0 requirements: .
- Bounded surface: Tauri capabilities and permissions
- Explicit exclusions: Excludes behaviors outside Tauri capabilities and permissions for requirements ; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-LAM-RISK-TEST-018` — The WP-I2-004 exit gate must run and pass the R-18 'Negative command tests' failure or boundary test for IPC capability overreach, preserve raw evidence tied to the executing package and commit, and block both WP-I2-004:EXIT and I15:RELEASE when the evidence is absent, stale, mismatched, or failing. (source: 19-RISK-REGISTER-EXPANDED.md / R-18)

## Technical prerequisites

- `WP-I0-004` via `REQUIRES_COMPONENT_DECISION` — WP-I2-004 requires the WP-I0-004 component/path decision before exact implementation locations can be finalized.
- `WP-I2-001` via `REQUIRES_COMPONENT_DECISION` — Tauri capabilities and permissions require a bootstrapped shell and concrete command surface. WP-I2-004 consumes the finalized Tauri decision owned by WP-I2-001.
- `WP-I2-003` via `REQUIRES_COMPONENT_DECISION` — WP-I2-004 consumes the finalized Platform WebView dependencies decision owned by WP-I2-003.

## Direct dependents

`WP-I3-003`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements ; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements ; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements ; existing Codebase evidence: none recorded.
- Tests: Run reviewed fixtures for requirements ; verification methods include . Risk-gate obligations: R-18/CAN-LAM-RISK-TEST-018 executes 'Negative command tests'.
- Failure cases: Failures must preserve authoritative state for requirements ; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements  pass and completion evidence is recorded. Risk-gate exit: raw PASS evidence tied to WP-I2-004 and its implementation commit is required for R-18; absence or failure blocks WP-I2-004:EXIT and I15:RELEASE.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
