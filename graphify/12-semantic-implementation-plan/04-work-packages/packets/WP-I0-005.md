# WP-I0-005 — Toolchain inventory

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Implement the reviewed Toolchain inventory for requirements CAN-MISSION-I0-006 covering 1 requirements: During read-only I0 inspection, the implementation team must.
- Bounded surface: Toolchain inventory
- Explicit exclusions: Excludes behaviors outside Toolchain inventory for requirements CAN-MISSION-I0-006; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-006` — During read-only I0 inspection, the implementation team must record the existing Node, package-manager, Rust, Python, media, and platform toolchain manifests and report unavailable or ambiguous versions without installing or modifying anything. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-004` via `REQUIRES_DECISION` — Toolchain inventory must interpret the reviewed manifest and lockfile conflicts rather than assume a toolchain.

## Direct dependents

`WP-I0-007`, `WP-I10-003`, `WP-I15-006`, `WP-I2-001`, `WP-I2-001`, `WP-I3-008`, `WP-I3-012`

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-006; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-006; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements CAN-MISSION-I0-006; existing Codebase evidence: none recorded.
- Tests: Run reviewed fixtures for requirements CAN-MISSION-I0-006; verification methods include Given the existing repository and host, when toolchain inspe.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-006; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements CAN-MISSION-I0-006 pass and completion evidence is recorded.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
