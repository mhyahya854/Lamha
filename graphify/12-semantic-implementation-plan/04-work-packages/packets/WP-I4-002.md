# WP-I4-002 — Incremental scan and watcher reconciliation

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I4`
- Objective: Implement and verify the bounded incremental scan and watcher reconciliation surface.
- Bounded surface: Incremental scan and watcher reconciliation
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (5)

- `CAN-LAM-ARCH-084` — Incremental rescanning must process only changed or new paths and report the scanned scope without rescanning the whole library. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 708-708)
- `CAN-LAM-ARCH-085` — File watching must detect changes within the authorized root and reconcile the index without mutating source files. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 709-709)
- `CAN-LAM-ARCH-086` — External change detection must reconcile the index with the current filesystem on the next scan and report mismatches without altering source files. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 710-710)
- `CAN-LAM-ARCH-375` — The watcher must detect changes within authorized roots and reconcile the index without modifying source files. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1394-1394)
- `CAN-LAM-EVENT-060` — When consistency detection compares the event folder with the event node, Lamha must report the mismatch and keep the folder and node unchanged until a review decision. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1899-1899)

## Technical prerequisites

- `WP-I4-001` via `REQUIRES_INDEX` — Watcher reconciliation reuses scanner identity and indexing rules.

## Direct dependents

`WP-I4-011`

## Contracts and schemas

- IPC commands: `scan.watcher_reconcile`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for incremental scan and watcher reconciliation; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for incremental scan and watcher reconciliation; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The incremental scan and watcher reconciliation objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
