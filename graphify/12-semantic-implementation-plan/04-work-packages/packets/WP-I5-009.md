# WP-I5-009 — Asset inspector

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I5`
- Objective: Implement and verify the bounded asset inspector surface.
- Bounded surface: Asset inspector
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (15)

- `CAN-LAM-ARCH-188` — When a review item offers Conflict warnings in 20.6 Inspector capabilities, the selected decision must produce a visible, persisted proposal-state transition while leaving authoritative metadata unchanged until an allowed approval action commits it; rejected invalid transitions must preserve the prior state. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1546-1546)
- `CAN-LAM-ARCH-192` — When the 20.6 Inspector capabilities workflow reads or changes Open sidecars, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1551-1551)
- `CAN-LAM-ARCH-195` — When the 20.6 Inspector capabilities workflow reads or changes Sidecar health, it must preserve the value, authority, and provenance in the appropriate durable record and expose the resulting revision to the derived index. Missing, malformed, or conflicting input must be reported without discarding the last valid value. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1556-1556)
- `CAN-LAM-ASSET-138` — When the user invokes Asset JSON in originals, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2001-2001)
- `CAN-LAM-DUPLICATE-002` — When the user invokes Side-by-side comparison for duplicates in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 687-687)
- `CAN-LAM-EDIT-003` — When the user invokes Tag editing in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 691-691)
- `CAN-LAM-EVENT-016` — When the user invokes Event assignment in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 692-692)
- `CAN-LAM-EVENT-017` — When the user invokes Open in event in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 697-697)
- `CAN-LAM-FOLDER-014` — When the user invokes Open in filesystem in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 695-695)
- `CAN-LAM-GOV-062` — When the user invokes Required source assets in 1.1 Codebase, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 148-148)
- `CAN-LAM-GOV-237` — When the user invokes Treat in 16.1 Asset bundle, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1002-1002)
- `CAN-LAM-GOV-240` — When a review item offers Preserve all values in 16.3 Conflicts, the selected decision must produce a visible, persisted proposal-state transition while leaving authoritative metadata unchanged until an allowed approval action commits it; rejected invalid transitions must preserve the prior state. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1026-1026)
- `CAN-LAM-GOV-242` — When a review item offers Show sources in 16.3 Conflicts, the selected decision must produce a visible, persisted proposal-state transition while leaving authoritative metadata unchanged until an allowed approval action commits it; rejected invalid transitions must preserve the prior state. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1028-1028)
- `CAN-LAM-GOV-244` — When a review item offers Record decision in 16.3 Conflicts, the selected decision must produce a visible, persisted proposal-state transition while leaving authoritative metadata unchanged until an allowed approval action commits it; rejected invalid transitions must preserve the prior state. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1030-1030)
- `CAN-LAM-META-025` — When the user invokes Metadata inspector in 7. Asset Viewer, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 688-688)

## Technical prerequisites

- `WP-I3-005` via `REQUIRES_SCHEMA` — The asset inspector requires validated authoritative and mirror sidecar reads with degraded-state reporting.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for asset inspector; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for asset inspector; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The asset inspector objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
