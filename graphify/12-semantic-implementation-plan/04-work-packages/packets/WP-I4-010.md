# WP-I4-010 — Asset and path index

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I4`
- Objective: Implement and verify the bounded asset and path index surface.
- Bounded surface: Asset and path index
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (17)

- `CAN-LAM-ARCH-147` — When a configured library encounters Required storage during 14.3 Materialization, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1112-1112)
- `CAN-LAM-ARCH-148` — When a configured library encounters Keep linked during 14.4 Dragging existing folders, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1122-1122)
- `CAN-LAM-ARCH-149` — When a configured library encounters Normalize name during 14.4 Dragging existing folders, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1123-1123)
- `CAN-LAM-ARCH-151` — When a configured library encounters Merge during 14.4 Dragging existing folders, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1125-1125)
- `CAN-LAM-ARCH-376` — When the user uses Timeline in Phase 5 — Asset API Replacement, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1403-1403)
- `CAN-LAM-ASSET-189` — When the user invokes Asset details in Phase 5 — Asset API Replacement, the viewer must apply or display the behavior for the selected asset, expose the resulting UI state, and preserve the original media when the operation or format is unavailable. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1404-1404)
- `CAN-LAM-FOLDER-003` — When a configured library encounters Internal drives during 1. Final Product Identity, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 32-32)
- `CAN-LAM-FOLDER-006` — When a configured library encounters Folder management during 2.1 Local-first operation, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 299-299)
- `CAN-LAM-FOLDER-023` — When a configured library encounters Link existing folders during 14.2 Draft / Safe mode, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1092-1092)
- `CAN-LAM-FOLDER-024` — When a configured library encounters Folders to create during 14.3 Materialization, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1104-1104)
- `CAN-LAM-FOLDER-026` — When a configured library encounters Folders to move during 14.3 Materialization, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1106-1106)
- `CAN-LAM-FOLDER-027` — When a configured library encounters Offline drives during 14.3 Materialization, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 1111-1111)
- `CAN-LAM-FOLDER-051` — When a configured library encounters External-drive differences during 37. Cross-Platform Support, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 2180-2180)
- `CAN-LAM-FOLDER-076` — When a configured library encounters Cross-drive interruption during 26. Risk Management, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1728-1728)
- `CAN-LAM-FOLDER-077` — When a configured library encounters External-drive identity during 26. Risk Management, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1729-1729)
- `CAN-LAM-FOLDER-078` — When a configured library encounters Cross-platform paths during 26. Risk Management, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 1736-1736)
- `CAN-LAM-GOV-126` — When a configured library encounters Do not assume paths during 6. Resolve the Real Repository First, Rust must apply authorized-root and access-mode rules, expose the resulting root or asset state, and leave files unchanged if validation, permission, or I/O checks fail. (source: 03-HOW-WE-WILL-KEEP-DELETE-AND-CHANGE.md / lines 476-476)

## Technical prerequisites

- `WP-I3-008` via `REQUIRES_INDEX` — Path lookup uses the derived SQLite schema.

## Direct dependents

`WP-I4-012`, `WP-I5-001`, `WP-I5-002`, `WP-I5-007`, `WP-I6-004`

## Contracts and schemas

- IPC commands: `assets.get`, `assets.locate`, `assets.open`, `assets.page`, `assets.selection_summary`
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for asset and path index; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for asset and path index; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The asset and path index objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
