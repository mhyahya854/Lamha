# WP-I3-007 — Authoritative schema migrations

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I3`
- Objective: Implement the reviewed Authoritative schema migrations for requirements  covering 0 requirements: .
- Bounded surface: Authoritative schema migrations
- Explicit exclusions: Excludes behaviors outside Authoritative schema migrations for requirements ; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-LAM-RISK-TEST-008` — The WP-I3-007 exit gate must run and pass the R-08 'Schema fixtures' failure or boundary test for Future/corrupt schema loss, preserve raw evidence tied to the executing package and commit, and block both WP-I3-007:EXIT and I15:RELEASE when the evidence is absent, stale, mismatched, or failing. (source: 19-RISK-REGISTER-EXPANDED.md / R-08)

## Technical prerequisites

- `WP-I0-004` via `REQUIRES_COMPONENT_DECISION` — WP-I3-007 requires the WP-I0-004 component/path decision before exact implementation locations can be finalized.
- `WP-I3-001` via `REQUIRES_SCHEMA` — Authoritative schema migrations require the closed record envelope and revision semantics.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements ; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements ; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Deliver reviewed implementations and evidence for requirements ; existing Codebase evidence: none recorded.
- Tests: Run reviewed fixtures for requirements ; verification methods include . Risk-gate obligations: R-08/CAN-LAM-RISK-TEST-008 executes 'Schema fixtures'.
- Failure cases: Failures must preserve authoritative state for requirements ; invalid input returns typed errors and no partial commit.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: Exit only when the reviewed fixtures for requirements  pass and completion evidence is recorded. Risk-gate exit: raw PASS evidence tied to WP-I3-007 and its implementation commit is required for R-08; absence or failure blocks WP-I3-007:EXIT and I15:RELEASE.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
