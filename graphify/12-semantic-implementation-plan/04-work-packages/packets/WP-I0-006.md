# WP-I0-006 — Build-output isolation decision

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I0`
- Objective: Define where later authorized build and test outputs may be written without creating a repository copy during the read-only provenance baseline.
- Bounded surface: Build-output isolation rules
- Explicit exclusions: Excludes behaviors outside Build-output isolation rules for requirements CAN-MISSION-I0-005; other package-owned capabilities and later-phase release/cleanup work.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (1)

- `CAN-MISSION-I0-005` — WP-I0-006 must define build-output isolation rules without creating a repository copy during the read-only provenance baseline; build or test execution requires a separately authorized implementation workspace after WP-I0-001 exits. (source: LAMHA ONE-SHOT SEMANTIC PLANNING REPAIR MISSION / attached mission)

## Technical prerequisites

- `WP-I0-001` via `REQUIRES_PROVENANCE` — Build-output isolation rules require the read-only repository provenance and integrity boundary to be recorded first; no repository copy or build is created by either package.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Contracts affected by requirements CAN-MISSION-I0-005; registry references are resolved in the package packet.
- Schemas affected: Schemas affected by requirements CAN-MISSION-I0-005; schema paths are resolved in the package packet.

## Delivery and proof

- Deliverables: Graphify-only decision recording authorized future output roots and forbidden write locations.
- Tests: Validate that the decision authorizes no current build, test, cache, generated-code, package-manager, or duplicate-tree output.
- Failure cases: Failures must preserve authoritative state for requirements CAN-MISSION-I0-005; invalid input returns typed errors and no partial commit.
- Rollback/recovery: Not applicable because the package records a decision only.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The later writable boundary is explicit and no workspace, copy, build, test, cache, or generated output was created.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
