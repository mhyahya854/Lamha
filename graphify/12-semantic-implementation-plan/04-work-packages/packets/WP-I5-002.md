# WP-I5-002 — Timeline navigation

Canonical source: `semantic-plan-source/packages/work-packages.json` plus the reviewed membership and dependency registries.

## Execution boundary

- Phase: `I5`
- Objective: Implement and verify the bounded timeline navigation surface.
- Bounded surface: Timeline navigation
- Explicit exclusions: Unrelated capabilities; later release/cleanup work; application-wide refactors.
- Commit boundary: One bounded commit containing only this package and its directly required contract/schema/test changes.

## Canonical requirements (8)

- `CAN-LAM-ARCH-061` — When the user uses Smooth date scrolling in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 651-651)
- `CAN-LAM-ARCH-062` — When the user uses Date jump controls in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 652-652)
- `CAN-LAM-ARCH-063` — When the user uses Adjustable grid size in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 653-653)
- `CAN-LAM-ARCH-064` — When the user uses Fast thumbnail loading in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 654-654)
- `CAN-LAM-ASSET-056` — When the user uses Missing-media indicators in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 664-664)
- `CAN-LAM-EVENT-015` — When the user uses Event browsing in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 658-658)
- `CAN-LAM-FOLDER-012` — When the user uses Folder browsing in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 657-657)
- `CAN-LAM-FOLDER-013` — When the user uses Offline-drive indicators in 6. Gallery and Timeline, the interface must expose the resulting selection, position, or view state from local data, remain keyboard-operable, and retain the prior stable view if loading fails. (source: 01-EVERYTHING-WE-ARE-KEEPING.md / lines 663-663)

## Technical prerequisites

- `WP-I4-010` via `REQUIRES_DERIVED_INDEX` — Timeline navigation requires capture-time and stable asset rows from the derived asset index.

## Direct dependents

None.

## Contracts and schemas

- IPC commands: None verified for this package.
- Contracts affected: Explicitly listed in the generated packet from reviewed command-to-package references; NONE only when verified.
- Schemas affected: Explicitly listed in the generated packet from reviewed record-to-package references; NONE only when verified.

## Delivery and proof

- Deliverables: Production implementation for timeline navigation; updated affected contracts and records; focused tests.
- Tests: Focused success, boundary, and failure tests for timeline navigation; affected integration checks.
- Failure cases: Invalid input; authorization failure; revision conflict; cancellation or I/O failure where applicable.
- Rollback/recovery: No partial authoritative state; use the package-specific transaction/recovery contract for mutations.
- Completion evidence: Changed-file list, focused test commands/results, contract/schema diffs, and recovery evidence when applicable.
- Exit gate: The timeline navigation objective and failure tests pass with no unrelated package work included.

Execute only this bounded package. Do not start a dependent package until its technical prerequisites and exit gate are proven.
